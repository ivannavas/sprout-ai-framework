package io.github.ivannavas.sprout.monitoring.custom;

/** Scanning anchor for the custom-store test: only this package's {@code @UsageStore} is scanned. */
public final class CustomStoreApp {
    private CustomStoreApp() {
    }
}
