package io.github.ivannavas.sprout.scancontrib.entry;

// Entry point for ScanPackageContributorTest. Its package holds no components, so anything the
// container manages here must have come from a contributed scan package, not the default one.
public class ScanContribEntry {
}
